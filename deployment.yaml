apiVersion: v1
kind: Deployment
metadata:
  name: nodeapp
  labels:
    app: nodeapp
spec:
  containers:
    - name: nodeapp
      image: bskreddy18/my-repo:latest
      ports:
        - containerPort: 8080
